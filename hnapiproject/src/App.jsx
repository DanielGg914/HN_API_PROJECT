import SearchBar from './components/SearchBar';
import Headings from './components/Headings';

function App() {
  return (
    <div className="App">
        <SearchBar />
  
        <Headings />
    </div>
  );
}

// - useFetch -- 
// - nice styles
// - pagination (the next and prev functionality)
// - removeable links (should be removed using the reducer)

export default App;
